schema: spec-driven

context: |
  Tech stack: Vanilla TypeScript, Vite, SortableJS, CSS custom properties, localStorage
  No framework — all DOM is imperative (document.createElement, manual event listeners)
  Single-page app: index.html has #app, #board, #modal-overlay
  Board state is a single Board object; mutations call persist() then render()
  Types use `as const` object pattern (not enums) for Status and Priority
  Strict TypeScript (strict: true, noUnusedLocals, noUnusedParameters)
  CSS theming via :root custom properties (--bg, --surface, --text, --accent, etc.)
  No tests or linter currently configured
  Source files: src/main.ts, src/components/board.ts, src/models/types.ts, src/storage/storage.ts, src/styles/main.css
